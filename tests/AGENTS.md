# tests/AGENTS.md

- 改邏輯前先執行既有離線測試與規範檢查。
- 純函式測試不得使用 mock。
- 測試路徑使用 `process.cwd()` 與 `path.resolve()`，確保 Windows 相容。
- 驗證 ACT 02 `manual-label-slow` 與 ACT 04 `auto-label` 不共用同一流程。
- 驗證原生 Ribbon 使用工作語言，工具列用箭頭游標，模型繪圖與框選才用十字游標。
- 驗證 AI-DIM 紅色標註與外圍／深井聚合線分層。
- 驗證深井矩陣固定 8 個物件、6 個標準圖層、2 個非標準圖層。
- 驗證矩陣整合在 STEP 06 的同一個 CAD 畫面，不存在獨立插頁元件。
- 驗證主要 CAD 動畫播放速率固定為 0.5×。
- 驗證框選只建立資料上下文，不在所有步驟持續顯示選取框。

- 驗證淺灰模型空間與深色 CAD 框架分離。
- 驗證底圖包含實線、隱藏線、中心線與 45° Hatch。
- 驗證 STEP 07 使用整層框選，並驗證 m／cm／mm 轉換。
