# UX Hierarchy v2 Preview Note

The preview for `feat/ux-hierarchy-v2` is packaged as a GitHub Actions artifact.

- It builds the feature branch only.
- It does not deploy GitHub Pages.
- It does not change the production Pages environment.
- It is retained for 7 days and is used for review before any merge to `main`.
